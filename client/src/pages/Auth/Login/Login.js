import LoginForm from "../../../components/LoginForm"

const Auth = () => {
  return (
    <>
      {/* {this.state.hasAccount ? <LoginForm /> : <RegisterForm />} */}
      <LoginForm />
    </>
  )
}

export default Auth